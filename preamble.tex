% for greek language
\usepackage{xltxtra} 
\usepackage{xgreek} 
\setmainfont[Mapping=tex-text]{GFS Didot} 

% need this package to add figures
\usepackage{graphicx}

% need to add this because the default captions are very close to the table
\usepackage{caption}

% Need to define the build/ dir as the output dir of the package
\usepackage{minted}
\usemintedstyle{tango}

% Change listings names in greek
\renewcommand\listingscaption{Αποσπάσμα Κώδικα}
\renewcommand\listoflistingscaption{Λίστα Αποσπασμάτων Κώδικα}

% for the bibliography
\usepackage[language=english, backend=biber]{biblatex}
\addbibresource{references.bib}
